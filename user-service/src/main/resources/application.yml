server:
    port: 9004

spring:
    profiles:
        active: prod
    application:
        name: user-service
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
                format_sql: true
